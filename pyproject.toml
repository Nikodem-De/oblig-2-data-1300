[project]
name = "health-app"
version = "0.1.0"
description = "A simple health and BMI tracker"
authors = [{ name = "Nikodem", email = "denegos11@gmail.com" }]
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"